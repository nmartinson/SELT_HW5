%h1 The following movies containing "#{@searchTerm}" were fround on TMDb

= form_tag add_tmdb_path, :method => :post do
  %table#movies
    %thead
      %tr
        %th Movie Title
        %th Rating
        %th Release Date
        %th Select
    %tbody
      - @matching_movies.each do |movie|
        %tr
          %td= movie[:title]
          %td= movie[:rating]
          %td= movie[:release_date]
          %td= check_box_tag "tmdb_movies[#{movie[:tmdb_id]}]"
 
  = submit_tag 'Add Selected Movies'
